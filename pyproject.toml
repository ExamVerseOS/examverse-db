[project]
name = "examverse-db"
version = "0.1.0"
description = "Shared schema layer for ExamVerseOS"
requires-python = ">=3.10"

dependencies = [
    "sqlalchemy",
    "psycopg2-binary"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"